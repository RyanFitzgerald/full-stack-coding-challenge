import styled from 'styled-components';

const Icon = styled.div`
  bottom: 0;
  height: 100%;
  left: 0;
  position: absolute;
  top: 0;
  width: 50px;

  svg {
    margin-top: 12px;
  }
`;

export default Icon;